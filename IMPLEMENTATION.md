# 单目标斜向抓取与底部投放：离线实现

本目录是独立候选副本，不是工作电脑正在使用的工程。没有启动实艇、修改真实标定或推送 GitHub。

## 已实现的动作

沿用群体发现（至少两只扇贝、最近五帧三次确认），锁定群内最高置信度单框；同分优先水平居中。之后以框重叠和距离关联同一个目标，关联不明确就按丢失处理，不按新的置信度排名换框。

水平稳定三帧后接近。选中框下边缘首次越过画面 80% 即停止前进，当前仍越线且居中、最近五个新帧四次达标才开爪。重复帧不计数。接近时 65% 以前前进 0.20，之后 0.10，旧群中心 70% 不再限制新模式。

开爪指令完成并经过标定等待后，前进 0.10、下降 0.35。偏离水平容差时停前进但保留下潜；首次目标漏检或框在底边被截断后，锁存为本次只垂直下降。视频故障与目标出画分开处理。抓取下降硬超时 10 秒，不自动加力，深度跳变/遥测过期保护保留。

深度稳定推测触底后执行：

`艇体回中 → 闭爪 → 抬爪 → 张爪 → 落料等待 → 闭爪 → 抬爪复位 → 艇体上浮约15厘米并稳定`

所有机械步骤串行，复位完成才计一次循环，前两次离底后重新观察当前群体并选框，第三次离底后离开当前群体再搜索。当前群体无可用目标时恢复搜索，不盲抓凑数。三次循环不是三只贝壳成功入网的证明。

## 执行反馈与保护

- `SetGripper` 保留 OPEN=1、CLOSE=2；新增 RAISE=3、RESET=4 和请求/响应 `request_id`。旧调用者可留空请求标识，由网关生成。ROS 类型已经改变，相关包和调用端必须一起重新构建，不能混用旧生成接口。
- 新话题 `/rov/control/gripper_execution` 带请求标识、来源、动作、时间戳和 accepted/running/completed/failed/cancelled 状态。客户端只接受当前请求，拒绝过期、重复或其他请求的完成消息。
- 双自由度每一步舵机命令均等待非阻塞 MAVLink ACK，最多2秒；拒绝/超时取消余下步骤且不重发。最后一步确认后才计标定等待。运动看门狗、遥测、急停继续运行。
- 服务“接受”不表示执行结束；“完成”也只表示命令序列和标定等待结束，**没有测到物理最高位，也没有确认抓牢或入网**。
- 硬件反馈过期、动作失败/取消均中止，不自动反向复位、张爪补救或重试。机械序列中按正常结束键也改为中止，不带着未复位的爪子自动回收。
- 投放期间持续艇体各轴回中，交给 ALT_HOLD；不主动压底，不保证物理静止。
- MAVLink 的舵机 ACK 本身没有本项目的请求标识，因此完整流程期间必须禁止 QGC、手柄或其他程序同时发舵机命令。

## 配置与入口

`ros2_ws/src/rov_competition/config/cluster_collection.yaml` 已切换 `target_bottom_line`，新增可调初值：

| 参数 | 初值 |
| --- | --- |
| target_bottom_line_y_ratio | 0.80 |
| target_near_bottom_y_ratio | 0.65 |
| grasp_forward_command | 0.10 |
| grasp_descent_command | 0.35 |
| grasp_descent_timeout_s | 10.0 秒 |
| transfer_to_net_wait_s | 1.0 秒落料等待 |

旧 image_line/union_area/both 仍保留；旧大连、RST 的开闭爪行为保留。新机械结构必须使用 `dual_dof`，一路只开闭，一路只抬起/复位，禁止复用旧 RST 两路联动开闭逻辑。

待标定模板：`ros2_ws/src/rov_competition/config/gripper.dual_dof.uncommissioned.yaml`。其中通道、允许 PWM 范围、动作端点/步长、节拍和四个等待时间均未填。请不要把测试代码里的合成数值当作真实参数。

模板只包含 gripper 部分，未来需合并到**台架配置副本**，保留真实工程原来的其他字段和旧档案。未标定参数、重叠通道、S1..S8、实际飞控映射到推进器的输出、越界 PWM 均不能启用新流程。最高位和复位端点必须不同。

台架工具新增独立 `--action open|close|raise|reset`。`--pwm N --from-pwm N` 只发一个明确范围内的值，且与操作员核对的当前 PWM 差不得超过该动作配置步长；不扫描通道、不寻找堵转位置。没有位置反馈，当前位置需要人工核对。

仅预览（不连接 MAVLink）：

```bash
ros2 run rov_competition rov_gripper_test dual_dof --config /完整路径/台架副本.yaml
```

未来台架测试仍须推进器物理断开、飞控上锁、危险区无人，并按每个动作单独确认。旧 `scripts/start_gripper_test.sh` 已支持 dual_dof 和这些参数。单步/单动作记录不允许激活；四个动作均经人工验收、逐步 ACK 成功、记录与配置签名一致后，才可人工用原 `rov_field_setup gripper activate` 流程激活（确认词 `ACTIVATE DUAL DOF GRIPPER`）。本次未执行。

将来完成部署、接口构建和标定后：

- 原无机械爪入口仍是 `scripts/start_cluster_collection_test.sh`，使用新接近判据，但到线只停止，不抓取。
- 完整新入口为 `scripts/start_cluster_grasp_test.sh`，强制要求新判据、dual_dof 和有效标定证据；参数变化后旧证据失效。启动前必须人工确认抬爪处于标定复位位置。

**当前不要直接用独立候选副本下水。** 80%、前进/下降力度、10秒以及新结构动作时间都没有实机验证。

## 观察与离线验证

独立画面进程增加黄色选中框、置信度、框下边缘比例和只垂直下降标记；紫色线为80%触发线。状态显示抬爪/投放/复位阶段和循环序号。事件与抓取日志记录请求标识、反馈状态、选中框和底边比例。原始录像仍不包含这些覆盖层。

新增测试：`test_target_grasp.py`、`test_dual_gripper.py`、`test_gripper_ros_client.py`。包括完整三次模拟循环、逐步 ACK、延迟接受/完成、过期反馈、失败/取消、视频与目标丢失的区别、复位之前禁止上浮、无目标不盲抓及标定证据变更。

验证在工作电脑 `/tmp/rov_grasp_20260912_CmABcx/candidate` 隔离目录进行，使用已有 Python 环境和系统 ROS Humble；没有在原工程构建或运行控制节点。`source_before.tgz` 是修改前的源码备份，不包含权重或真实配置。

最终结果（2026-09-13）：435 项 pytest 全部通过，包含 `ROS_DOMAIN_ID=232`、`ROS_LOCALHOST_ONLY=1` 下的画面阻塞隔离测试。ROS 两个包构建通过，SetGripper 和 GripperExecutionStatus 接口生成、启动脚本语法检查通过。colcon 自身报告0项注册测试、0错误；435项是另行运行的 pytest，不能把这两个数混为一谈。

验证原始日志保存在 `validation/`。`implementation.patch` 是相对修改前源码快照的补丁，包含新文件；仅供审查和以后部署，未应用到实际工程。部署前仍应检查实际工程有没有队友的新改动，不能直接整目录覆盖。
