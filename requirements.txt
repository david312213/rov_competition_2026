# 仅包含 ROS 2 apt 仓库之外的 Python 依赖。
# OpenCV 4.12 开始在 Python 3.10 上要求 NumPy 2，但 Ubuntu 22.04/ROS Humble
# 中仍有按 NumPy 1.x ABI 编译的视觉模块。固定这一对已验证版本，避免
# “_ARRAY_API not found” 和进程崩溃。
numpy==1.26.4
opencv-python==4.11.0.86
# YOLO26 权重记录的训练版本为 Ultralytics 8.4.117，因此部署端固定同版。
ultralytics==8.4.117
pymavlink>=2.4.41,<3
# 数据集采集使用独立小窗口可靠获取按下、松开和失去焦点事件。
pygame>=2.5,<3
# 抽帧桌面软件使用 Tk 原生拖放；体积远小于完整 Qt 运行时。
tkinterdnd2==0.6.2
# 一键脚本用 MAVProxy 将 BlueOS 的一份 MAVLink 分给 ROS 和 QGC。
MAVProxy==1.8.74
PyYAML>=6,<7
pytest>=8,<9
# ROS 2 Humble 的 colcon-core 不支持 setuptools 80+。
setuptools>=59.6,<80
# install.sh 会在虚拟环境中生成独立 colcon 入口。
# colcon-core 的运行元数据要求这两个 pytest 插件，显式声明可避免
# --system-site-packages 环境中出现“pip check 缺失”。
pytest-repeat>=0.9,<1
pytest-rerunfailures>=13,<17
colcon-common-extensions>=0.3,<1
