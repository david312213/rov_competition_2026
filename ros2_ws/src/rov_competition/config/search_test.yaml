# 0.2.0rc2 搜索—接近水池测试参数
#
# 启动时只输入固定下潜 power，不再输入目标深度。
# 程序持续下潜，当最近 3 秒的深度变化不超过 0.05m 时，
# 记录疑似池底，先上浮一个小间隙并稳定定深，再进入扫描。

search_test:
  # 没有人工目标深度后，这是仍然保留的硬停止上限。
  # 换水池后必须按实际场地审核，不要把它当成池底深度。
  maximum_operation_depth_m: 20.0
  bottom_detection_stable_s: 3.0
  bottom_detection_depth_tolerance_m: 0.05
  bottom_detection_minimum_descent_m: 0.10
  # 在向下推力下稳定 1 秒即回中，之后由 ALT_HOLD 定深到完整 3 秒确认结束。
  bottom_neutral_confirmation_s: 1.0
  # 触底确认后先上浮 0.10m，稳定定深后才开始旋转，避免压底摩擦。
  # 现场若只需 5cm，只把 bottom_clearance_m 改为 0.05。
  bottom_clearance_m: 0.10
  bottom_clearance_tolerance_m: 0.02
  # 实艇日志表明 +0.20 不足以上浮；离底阶段使用独立的 +0.40。
  bottom_clearance_up_command: 0.40
  bottom_clearance_settle_s: 1.0
  bottom_clearance_timeout_s: 15.0

  # 下列参数仍用于人工标定中按 R 后回到本轮搜索深度。
  descent_tolerance_m: 0.05
  descent_gain: 0.80
  descent_minimum_command: 0.10
  descent_settle_s: 1.0
  # 若深度始终没有出现稳定平台，超时后急停，不会无限下潜。
  descent_timeout_s: 45.0

  # 检测帧超过 hold 时间未更新就回中，但容忍偶发解码卡顿；
  # 只有连续超过 abort 时间才中止整个任务。
  perception_hold_timeout_s: 1.0
  perception_abort_timeout_s: 5.0
  # 新检测帧之间最多短时保持上一条纯偏航对准命令。
  tracking_command_hold_s: 0.20

  # 键2对应的向右偏航方向。这里是归一化 power，不是角度。
  scan_yaw_command: 0.40
  scan_angle_deg: 360.0
  scan_report_step_deg: 15.0
  scan_tolerance_deg: 3.0
  scan_timeout_s: 120.0
  maximum_yaw_step_deg: 45.0
  maximum_search_cycles: 3

  # 没找到目标时，使用键W对应方向。
  advance_forward_command: 0.40
  advance_duration_s: 2.0

  aim_x_ratio: 0.50
  # 与带框画面的 GRASP AIM 保持一致；实际运行会优先使用
  # 当前 autonomy(.local).yaml 中的抓取瞄准点。
  aim_y_ratio: 0.70
  # 前视画面未镜像：+1 表示左框左转、右框右转。
  # 只有实测确认画面被镜像时才改为 -1。
  image_yaw_sign: 1
  # 普通前视画面：框在瞄准点上方时上升，让框在画面中下移；
  # 框在瞄准点下方时下潜。实测反向时改为 +1。
  image_vertical_sign: -1
  horizontal_tolerance: 0.08
  vertical_tolerance: 0.10
  realign_threshold: 0.12
  vertical_realign_threshold: 0.15
  alignment_frames: 3
  yaw_gain: 0.80
  minimum_yaw_command: 0.10
  maximum_yaw_command: 0.20
  vertical_gain: 0.80
  minimum_vertical_command: 0.30
  maximum_vertical_command: 0.40

  far_area_ratio: 0.07
  stop_area_ratio: 0.10
  far_forward_command: 0.20
  near_forward_command: 0.10
  approach_yaw_command: 0.10
  approach_vertical_command: 0.30
  maximum_target_area_ratio: 0.80

  acquisition_window_frames: 5
  acquisition_required_hits: 3
  loss_required_frames: 5
  loss_confirmation_s: 0.80
  # 对准阶段一两帧空框不再反复切换状态；接近阶段仍立即停车。
  alignment_loss_grace_frames: 2
  alignment_loss_grace_s: 0.20
  finish_window_frames: 5
  finish_required_hits: 4
  maximum_tracking_jump_ratio: 0.25
  minimum_tracking_iou: 0.05

  # 稳定对准后进入人工抓取标定时使用；不影响自动接近模式。
  manual_initial_command: 0.10
  manual_minimum_command: 0.05
  manual_maximum_command: 0.40
  manual_command_step: 0.01
  minimum_success_samples: 5

  # 实艇日志表明 +0.20 无法上浮。距目标较远时接近
  # 0.60，接近启动深度后逐步降至 0.40。
  return_gain: 1.00
  return_minimum_command: 0.40
  return_maximum_command: 0.60
  return_tolerance_m: 0.05
  return_settle_s: 1.0
  return_timeout_s: 60.0
